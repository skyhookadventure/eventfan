import { Page } from "../types/PageViewProps";

const mockPage: Page = {
  options: undefined,
  name: "title",
  properties: {
    path: "/path",
    title: "title",
    url: "https://www.example.com/path",
  },
};

export default mockPage;
