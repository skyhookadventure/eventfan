import { User } from "../types/IdentifyProps";

const mockUser: User = {
  options: undefined,
  traits: {
    email: "test@gmail.com",
    firstName: "First Name",
  },
  userId: "userID",
};

export default mockUser;
